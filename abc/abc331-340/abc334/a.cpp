#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); i++)
using namespace std;
typedef long long ll;

int main()
{
  int b, g;
  cin >> b >> g;
  if (b > g)
    cout << "Bat" << endl;
  if (b < g)
    cout << "Glove" << endl;
}
