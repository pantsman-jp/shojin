#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); i++)
using namespace std;
typedef long long ll;

int main()
{
  int s;
  cin >> s;
  if (200 <= s && s < 300)
    cout << "Success" << endl;
  else
    cout << "Failure" << endl;
}
