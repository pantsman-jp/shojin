#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); i++)
using namespace std;
typedef long long ll;

int main()
{
  string p;
  int l;
  cin >> p >> l;
  if (l <= (int)p.size())
    cout << "Yes" << endl;
  else
    cout << "No" << endl;
}
